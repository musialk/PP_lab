#include <stdio.h>

int main ()
{
int delta;
if(delta >=0)
 {
 	if (delta>0)
 	printf ("dwa rozwiazania");	
}
else 
printf ("brak rozwiazan");
return 0;
}
// pêtla if else, najpierw jest sprawdzane 1 wyra¿enie, dopiero póŸniej kolejne
//np
// if (x<y)
//z = x;
// else
// z = y; odpowiednik tego zapisu z= (x<y) ? x : y;

